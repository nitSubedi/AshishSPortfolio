# Claude Code Instructions

## Git & Pushing

Always push using SSH:

```
git push origin main
```

The remote is configured as `git@github.com:nitSubedi/AshishSPortfolio.git`. Do not use HTTPS or tokens — SSH is set up and working.
